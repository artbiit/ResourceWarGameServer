# 모든 텍스트 파일은 LF로 설정
* text=auto eol=lf
*.md text eol=lf
*.json text eol=lf

# 바이너리 파일은 라인 종료 방식 변경 안 함
*.dll binary
*.exe binary
*.png binary
*.jpg binary

# Unix 실행 파일
*.sh eol=lf

# Unity 관련 파일 (필요한 경우)
*.unity eol=lf
*.meta eol=lf

# Windows에서 실행 가능한 파일은 CRLF 유지
*.bat eol=crlf
*.cmd eol=crlf

